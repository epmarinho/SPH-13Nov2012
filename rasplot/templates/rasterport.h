#include "rasterfile.h"
#define COLORDEPTH 24
/* set number of pixels per dimension */
extern unsigned NX[DIM];
/* set image frame */
extern unsigned X[2][DIM];
/* set the pixmap */
extern unsigned char *pixel;
